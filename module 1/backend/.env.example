# Port the backend server listens on
PORT=4000

# Maximum allowed upload size, in bytes (default: 10 MB)
MAX_UPLOAD_BYTES=10485760

# Folder where uploaded .eml files are temporarily stored before parsing
UPLOAD_DIR=uploads
